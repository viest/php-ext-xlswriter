# 可选项

> _此页面正在补充中（v1.6.0+ 新功能）。_

## 方法签名

`Table::noHeaderRow()`

`Table::noAutofilter()`

`Table::noBandedRows()`

`Table::bandedColumns()`

`Table::firstColumn()`

`Table::lastColumn()`

`Table::totalRow()`
