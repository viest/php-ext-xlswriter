# Conditional format

> _Documentation in progress (v1.6.0+)._

## Methods

Apply a `Vtiful\Kernel\ConditionalFormat` rule to a single cell or a range:

`Excel::conditionalFormatCell(int $row, int $column, $cfHandle)`

`Excel::conditionalFormatRange(string $range, $cfHandle)`
