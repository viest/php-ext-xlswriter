# Get style format

> _Documentation in progress (v1.6.0+)._

## Methods

`Excel::getStyleFormat(int $styleId): array` — resolves a style id (from `nextRowWithFormula()`) into an associative array of font/fill/border/alignment/protection.
