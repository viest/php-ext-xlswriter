# 数据条

> _此页面正在补充中（v1.6.0+ 新功能）。_

## 方法签名

`ConditionalFormat::barColor(int $color)`

`ConditionalFormat::barOnly(bool $only = true)`

`ConditionalFormat::barSolid(bool $solid = true)`

`ConditionalFormat::dataBar2010(bool $on = true)`

`ConditionalFormat::barNegativeColor(int $color)`

`ConditionalFormat::barBorderColor(int $color)`

`ConditionalFormat::barNegativeBorderColor(int $color)`

`ConditionalFormat::barNoBorder()`

`ConditionalFormat::barDirection(int $direction)`

`ConditionalFormat::barAxisPosition(int $position)`

`ConditionalFormat::barAxisColor(int $color)`
