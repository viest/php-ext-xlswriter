# Insert comment

> _Documentation in progress (v1.6.0+)._

## Methods

`Excel::insertComment(int $row, int $column, string $text)`

`Excel::insertCommentOpt(int $row, int $column, string $text, array $options)`

`Excel::showComment()`
