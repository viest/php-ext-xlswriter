# Unlock cells inside a protected sheet

> _Documentation in progress (v1.6.0+)._

## Methods

Apply `Format::unlocked()` to a Format and pass that format to `insertText` / `setRow` / `setColumn` for the cells that must stay editable.
