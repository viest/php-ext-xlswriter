# 行 / 列设置

> _此页面正在补充中（v1.6.0+ 新功能）。_

## 方法签名

`Excel::getRowOptions(int $row): array`

`Excel::getColumnOptions(int $column): array`

`Excel::getDefaultRowHeight(): float`

`Excel::getDefaultColumnWidth(): float`
