# Drop-down list

> _Documentation in progress (v1.6.0+)._

## Methods

Use `Validation::validationType(Validation::TYPE_LIST)` and `Validation::valueList(array)`, then `Excel::validation('A1:A10', $handle)`.
