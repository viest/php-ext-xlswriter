# 条件格式

> _此页面正在补充中（v1.6.0+ 新功能）。_

## 方法签名

通过 `Vtiful\Kernel\ConditionalFormat` 构造规则后，应用到单元格或区域：

`Excel::conditionalFormatCell(int $row, int $column, $cfHandle)`

`Excel::conditionalFormatRange(string $range, $cfHandle)`
